<% if user_signed_in? %>
  <li class="navbar-text"><%= number_to_currency(current_user.available_cred, :unit => '') %> Free Cred</li>
  <li>
  <%= link_to(current_user.username, user_page_path) %>
  </li>
<% else %>
  <li>
  <%= link_to('Sign Up', new_user_registration_path)  %>
  </li>
<% end %>
